import { baseURL } from "./axiosAdress";

const localApiName = {
  apiCategory: `api/category`,
  apiProduct: `api/product`,
  apiExtraInfo: `api/information`,
  apiFeedback: `api/feedback`,
  apiComment: `api/comment`,
};

export default localApiName;
